This repo will hold solutions to Project Euler problems.

You can run unit tests with PHPUnit.

To run them do as follows (PowerShell):

``` powershell
./vendor/bin/phpunit --bootstrap .\vendor\autoload.php .\tests\test.php
```

**WIP**